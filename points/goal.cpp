#include <SFML/System.hpp>

#include "player.h"
#include "goal.h"


bool hit (sf::Vector2f first, sf::Vector2f second, float threshold)
{
    // Вспомогательная функция.
    
    // Проверка того, что расстояние между точками first и second меньше threshold.
    // Возвращаемое значение: true (меньше) или false.
}

bool Goal::hit (Player player)
{
    // Проверка того, коснулся ли игрок цели.
    // Возвращаемое значение: true (коснулся) или false.
    
    // Можно ввести вспомогательную функцию hit.
}
